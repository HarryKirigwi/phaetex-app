{
  "name": "phaetex-solutions",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "predev": "powershell -NoProfile -Command \"New-Item -ItemType Directory -Force -Path 'public/widget' | Out-Null; if (Test-Path 'frontend/widget') { Copy-Item -Force -Recurse 'frontend/widget/*' 'public/widget/' }\"",
    "dev": "next dev",
    "prebuild": "powershell -NoProfile -Command \"New-Item -ItemType Directory -Force -Path 'public/widget' | Out-Null; if (Test-Path 'frontend/widget') { Copy-Item -Force -Recurse 'frontend/widget/*' 'public/widget/' }\"",
    "build": "next build",
    "start": "next start",
    "lint": "next lint"
  },
  "dependencies": {
    "next": "^15.0.0",
    "react": "^19.0.0",
    "react-dom": "^19.0.0",
    "recharts": "^3.8.0",
    "three": "^0.170.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "@types/react": "^19.0.0",
    "@types/react-dom": "^19.0.0",
    "@types/three": "^0.170.0",
    "autoprefixer": "^10.4.20",
    "eslint": "^10.0.1",
    "eslint-config-next": "^0.2.4",
    "postcss": "^8.4.49",
    "tailwindcss": "^3.4.15",
    "typescript": "^5.0.0"
  }
}
